import "./globals.css";

export const metadata = {
  title: "Inversa Scroll Animation | Codegrid",
  description: "Inversa Scroll Animation | Codegrid",
};

export default function Step2Layout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
